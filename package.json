{
  "name": "nid-giga-archive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check dist/app.js && node --check dist/project.js && node --check dist/theme.js && node --check dist/archive-search.js && node --check server.mjs && node --check dist/boot.js && node --check dist/preview-state.js && node --check dist/project-template.js && node --check dist/about-template.js && node --check dist/site-config.js && node --check scripts/build.mjs && npm run validate",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-archive.mjs",
    "benchmark": "node scripts/search-benchmark.mjs",
    "prestart": "npm run validate",
    "predev": "npm run validate",
    "build": "node scripts/build.mjs",
    "assets:fetch": "node scripts/fetch-downloads.mjs"
  },
  "description": "A searchable archive of gigamaps from NID Information Design and Universal Design.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Kuldeep-psd/nid-giga-archive.git"
  },
  "engines": {
    "node": "22.x"
  }
}
